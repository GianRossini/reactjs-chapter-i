{
  "compilerOptions": {
    "lib": ["DOM", "DOM.Iterable", "ESNext"], // Entende que estamos lidando com a DOM, adicionando tipagens que antes não exitiam
    "allowJs": true, // Podemos ter tanto arquivos js quanto ts
    "jsx": "react-jsx", // Informa que estamos usando jsx do React
    "noEmit": true, // Não emitir o código quando executando o build
    "strict": true, // Modo strict -> lidar com erros
    "moduleResolution": "node",
    "resolveJsonModule": true, // Permite importar arquivos JSON
    "isolatedModules": true,
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },

  "include": ["src"] // Onde o código vai estar
}
